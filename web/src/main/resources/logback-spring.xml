<?xml version="1.0" encoding="UTF-8"?><configuration>    <!-- 按天处理日志文件 -->    <property name="FILE_NAME_DATE_PATTERN" value="yyyy-MM-dd"/>    <property name="FILE_LOG_PATTERN"              value="%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] [%X{session_id}] %logger{50}: %msg%n"/>    <!--    日志文件的保存路径,首先查找系统属性-Dlog.dir,如果存在就使用其；否则，在当前目录下创建名为logs目录做日志存放的目录    <property name="LOG_HOME" value="${log.dir:-logs}/${APP_NAME}" />    -->    <property name="LOGS_FOLDER" value="${APP_LOG_HOME:-logs}"/>    <include resource="logback/default-base.xml"/>    <!--        logger：用来设置某一个包或具体的某一个类的日志打印级别、以及指定<appender>。<logger>仅有一个name必选属性，一个可选的level和一个可选的addtivity属性。            可以包含零个或多个<appender-ref>元素，标识这个appender将会添加到这个logger            name: 用来指定受此logger约束的某一个包或者具体的某一个类。            level: 用来设置打印级别，大小写无关：TRACE, DEBUG, INFO, WARN, ERROR, ALL和OFF，                还有一个特俗值INHERITED或者同义词NULL，代表强制执行上级的级别。如果未设置此属性，那么当前logger将会继承上级的级别。            addtivity: 是否向上级logger传递打印信息。默认是true。同<logger>一样，可以包含零个或多个<appender-ref>元素，标识这个appender将会添加到这个logger。        -->    <springProfile name="production">        <root level="INFO">            <appender-ref ref="ALL-FILE"/>            <appender-ref ref="ERROR-FILE"/>        </root>        <logger name="com.whw.footstones.log.LogFilter" level="INFO" additivity="false">            <appender-ref ref="API-FILE"/>        </logger>    </springProfile>    <springProfile name="!production">        <root level="INFO">            <appender-ref ref="CONSOLE"/>            <appender-ref ref="ALL-FILE"/>            <appender-ref ref="ERROR-FILE"/>        </root>        <logger name="com.whw.footstones.log.LogFilter" level="INFO" additivity="false">            <appender-ref ref="CONSOLE"/>            <appender-ref ref="API-FILE"/>        </logger>    </springProfile></configuration>